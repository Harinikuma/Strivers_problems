target = 8
arr = [4,5,3,6]

def findSum(target, arr):
    seen = {}
    for i in range(len(arr)):
        num = arr[i]
        bal = target - num
        if(bal in seen):
            return (seen[bal], i)
        seen[num] = i
print(findSum(target, arr))
